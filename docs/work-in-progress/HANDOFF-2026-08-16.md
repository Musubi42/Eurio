# HANDOFF — session du 2026-08-16

> Point de reprise. Ce qui a été livré, ce qui reste ouvert, et **la décision à
> prendre en premier**. Branche : `repo-cleanup`. Tout est poussé sur les deux
> remotes (github + codeberg).

## Où on en est en une phrase

Le Lab est redevenu utilisable de bout en bout — cohorte, bake, entraînement —
mais **l'entraînement ne tourne que dans un mode compute explicite** : la place
des runs sous Direction A est la décision qui bloque la suite.

## Livré

| Sujet | État |
|---|---|
| **ADR-004** — modèles de l'APK hors git, servis par MinIO | ✅ basculé, `fetchModelAssets` branché sur `preBuild`, vérifié sur Mac **et** PC |
| **Travail du PC récupéré** (B2/B5/B6 + tracker `pipeline-findings-and-debt.md`) | ✅ non commité depuis le 8 juillet, rapatrié sans conflit |
| **B1** — quota eBay split-brain | ✅ compteur et lecteurs sur le même fichier |
| **B4** — lien vers le détail d'un run depuis le drawer lab | ✅ avec repli local sur 404 |
| **B3** — galerie d'enrichissement lit le canonique (direction 1) | ✅ déployé, images en URL signée |
| **C5 dimensions lab** — cohortes/itérations écrivent au writer unique | ✅ `serving/lab_writes.py` |
| **Rail `jobs/`** — bookkeeping vers la DB locale | ✅ débloque le bake |
| Revue adversariale de la session | ✅ 8 constats, tous fondés, tous corrigés |

Détail et raisonnement : `docs/work-in-progress/scan-quality/pipeline-findings-and-debt.md`
et `docs/work-in-progress/local-sync/migration-direction-a.md`.

## ⛔ La décision à prendre en premier

**Où vivent `training_runs` / `epochs` / `steps` sous Direction A ?**

Lancer un entraînement depuis le shell standard échoue en
`attempt to write a readonly database` : `TrainingRunner.create_run_row` écrit
le canonique. Ce n'est pas un oubli de câblage — `store.resolve_local_state_db()`
signale déjà le nœud : ces tables sont **locales par nature** (le compute vit sur
la machine GPU, et le MVP R3 ne pousse au canonique que la métadonnée
d'itération) mais elles sont **JOINTES avec des tables canoniques**, donc un
split physique exige un `ATTACH` ou un arbitrage.

Trois options, développées dans `migration-direction-a.md` §C5 :

1. `training_runs` & co. → `local_state_store()`, avec `ATTACH` pour les lectures jointes.
2. Les pousser au canonique par `/ingest/*` — contredit « les tables lourdes ne
   sont jamais poussées ».
3. Assumer un **mode compute explicite** : la machine GPU travaille sur une DB
   locale inscriptible, seules les dimensions remontent.

C'est l'option 3 qui a servi de contournement le 2026-08-16, et elle a
fonctionné bout en bout (run complète, `completed` repropagé au canonique par
F09). La question est de savoir si on la formalise ou si on va vers (1).

## Reprendre le lab en 3 commandes

```bash
direnv allow
nix develop .#mac --command sqlite3 ml/state/eurio.replica.db \
  "VACUUM INTO 'ml/state/eurio.work.db'"        # ⚠️ jamais `cp` (WAL)
EURIO_DB_READONLY= EURIO_DB_PATH="$PWD/ml/state/eurio.work.db" go-task ml:api-prod
go-task front:dev                                # autre terminal
```

Une run de démonstration existe déjà : cohorte `aaf3fcd8f717` (« again »),
itération `4aaac6865ca9` — `completed`, **R@1 97,22 %**, artefacts dans
`ml/lab/iterations/4aaac6865ca9/` (checkpoint, TFLite, métriques par classe).

## Autres sujets ouverts (par ordre d'utilité)

1. **Exercice de restauration à froid** (lot 6 du chantier backup) — jamais fait.
   Une autre session y travaillait en parallèle ce jour-là. Tant qu'il n'est pas
   passé, on a des sauvegardes vérifiées à l'émission, jamais à la réception.
2. **ADR-004 étape 3** — `best.pt` et le dataset de détection restent dans git
   (git sert encore de transport Mac→PC pour eux). Le mécanisme est prouvé sur
   les deux machines ; il manque la bascule, **à vérifier sur le PC avant tout
   `git rm`**.
3. **Le 403 transitoire MinIO** (P1 de `storage-hardening`) — jamais résolu à la
   source, masqué par un retry. C'est le PC en cache froid qui le prend.
4. **Décision #5** de `storage-hardening` — les obverses canoniques Numista
   absentes du PC bloquent un entraînement complet là-bas (elles existent sur
   le Mac, d'où la run réussie).
5. **B1 annexe** — l'estimation « ~N appels » du preview eBay ignore les
   `get_item` d'hydratation : 47 estimés contre 4733 réels.
6. **Détracker `ml/shared/state/eurio.db`** — ancienne DB de quota, plus lue,
   encore dans git. Suppression de données = décision PO.
7. **`eurio-scrape-tor`** est `unhealthy` depuis ~4 semaines sur le VPS
   (pré-existant, non investigué).

## Ce que la session a appris (compilé en skills)

`.claude/skills/` — `eurio-data-writes`, `eurio-run-local`, `eurio-vps-deploy`,
`eurio-verify`. Elles existent parce qu'on a payé le prix de leur absence :
le flip Direction A posé par le devShell, l'image lean du VPS qui skippe un
routeur entier pour un import lourd, les pannes muettes, le piège WAL.

La leçon la plus transférable est dans `eurio-verify` : **un test qui ne peut
pas échouer ne prouve rien**. La passe de mutation a rattrapé cinq défauts dans
la session, dont un correctif à moi qui était purement du code mort.

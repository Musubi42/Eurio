"""Sources refacto — modular ingestion pipeline.

See `docs/sources-refacto/` for the full specification. Phase 1 lays
down `_base/` (this folder) and the eBay module as the first concrete
source. Subsequent phases add Catawiki, NumisCorner, CGB, and the
quality scoring pipeline.
"""

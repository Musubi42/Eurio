# Empty placeholder so the secrets/ directory exists at clone time.
# The actual credentials (minio_root_user, minio_root_password,
# eurio_app_user, eurio_app_password) are generated by bootstrap.sh
# and never committed.
